#include <iostream>
#include "Server.h"
#include "ServerManager.h"


using namespace std;

int main()
{
/*
    Server* pServer = new Server(3399);
    pServer->run();

    pServer->create();
    pServer->bind_s();
    pServer->listen_s(10);

    */

    return 0;
}
